# Frontend (Vite) - copy to .env (gitignored)
# VITE_API_URL is auto-proxied by vite.config.ts in dev

# Backend proxy
VITE_API_URL=http://localhost:5000

# Sentry (optional - leave empty to disable)
VITE_SENTRY_DSN=
VITE_APP_VERSION=0.0.0
